# القطعة 1-ب: الحسابات والمصادقة (التسجيل + الدخول)

> تُبنى فوق القطعة 1-أ. مكتوبة ومُختبرة. أسلوب معزول كما السابق.

## ما الذي تفعله هذه القطعة

تأخذ وحدة الأمان (1-أ) وتبني فوقها نظام حسابات كامل:

- **قاعدة بيانات + جدول `users`** — نخزّن تجزئة كلمة المرور فقط، لا كلمة المرور.
- **المسارات:**
  - `POST /auth/register` — حساب جديد
  - `POST /auth/login` — دخول، يرجّع access + refresh
  - `POST /auth/refresh` — تجديد access بدون إعادة دخول
  - `GET /auth/me` — بيانات المستخدم الحالي (محمي بتوكن)
- **بوابة `get_current_user`** — أساس "كل مستخدم يرى أفرانه فقط" في القطع القادمة.

## ما الذي عُولج من ملاحظات القطعة 1-أ

1. **`JWT_SECRET` صار يُقرأ من `.env`** عبر `config.py`. التطبيق يحذّرك عند الإقلاع
   إن كان السرّ ما زال القيمة المؤقتة.
2. **ثغرة نوع التوكن:** `decode_token` صار يفحص نوع التوكن. توكن `refresh`
   لم يعد يُقبل في المسارات المحمية (اختبار `test_refresh_token_cannot_be_used_as_access`).
3. **عدم تسريب وجود البريد:** رسالة خطأ الدخول واحدة للحالتين.

## كيف تشغّلها (3 دقائق)

```bash
# 1) بيئة افتراضية + المتطلبات
python3 -m venv venv && source venv/bin/activate
pip install -r requirements.txt

# 2) أنشئ .env من المثال وولّد سرّاً
cp .env.example .env
python3 -c "import secrets; print('JWT_SECRET=' + secrets.token_urlsafe(48))"
#   انسخ الناتج إلى .env مكان السطر القديم

# 3) شغّل الاختبارات (يجب أن تنجح كلها)
pytest tests/ -v

# 4) شغّل الخادم
uvicorn app.main:app --reload --port 8000
#   ثم افتح: http://localhost:8000/docs
```

## الانتقال إلى PostgreSQL لاحقاً (القطعة 3)

لا تغيّر أي كود. فقط في `.env`:
```
DATABASE_URL=postgresql://user:password@localhost:5432/kiln_platform
```
ثم: `pip install psycopg2-binary`. الباقي يعمل كما هو.

## الخطوة التالية (القطعة 1-ج / 2)
نموذج **الأفران (kilns)** مربوط بـ `owner_id`، ومسار يستقبل قراءات أردوينو
بمفتاح جهاز (`X-Device-Key`)، ثم الواجهة.
