# دليل النشر على Coolify (خطوة بخطوة)

نشرك يعتمد على: VPS + Coolify + GitHub. أي تعديل تدفعه لـ GitHub
يُنشر تلقائياً. إليك كل الخطوات بالترتيب.

## نظرة عامة على ما سنفعله
1. نرفع الكود الجديد إلى مستودع GitHub (Abd-server/web).
2. ننشئ قاعدة PostgreSQL في Coolify.
3. نضبط متغيّرات البيئة (JWT_SECRET, DATABASE_URL).
4. نعيد النشر (Redeploy) ونتأكد.

────────────────────────────────────────────────────────
## الخطوة 1: رفع الكود إلى GitHub
────────────────────────────────────────────────────────
افتح Terminal في مجلد المشروع (piece1b) ونفّذ:

```bash
cd ~/Desktop/piece1b

# لو المجلد غير مربوط بـ git بعد:
git init
git remote add origin https://github.com/Abd-server/web.git

# لو مربوط مسبقاً، تأكد من الـ remote:
git remote -v
```

ثم ادفع الكود (سيستبدل المحتوى القديم):

```bash
git add .
git commit -m "النسخة الجديدة: حسابات + أفران + إشعارات + سجل أحداث"
git branch -M main
git push -u origin main --force
```

> ملاحظة: `--force` يستبدل المحتوى القديم بالكامل. لو تبي تحتفظ
> بالقديم، أخبرني ونعمل فرعاً منفصلاً بدل ذلك.

────────────────────────────────────────────────────────
## الخطوة 2: إنشاء قاعدة PostgreSQL في Coolify
────────────────────────────────────────────────────────
1. في Coolify، افتح مشروعك (web > production).
2. اضغط **+ New** → اختر **Database** → **PostgreSQL**.
3. اتركه بالإعدادات الافتراضية واضغط **Create**.
4. بعد إنشائه، افتحه وانسخ **Postgres URL / Connection String**
   (يبدأ بـ postgresql://...). احفظه، سنحتاجه في الخطوة 3.
5. تأكد أن قاعدة البيانات والتطبيق في **نفس الـ network** (Coolify
   يربطهما تلقائياً عادة لو في نفس المشروع).

────────────────────────────────────────────────────────
## الخطوة 3: ضبط متغيّرات البيئة
────────────────────────────────────────────────────────
1. ولّد سرّاً قوياً على جهازك:
   ```bash
   python3 -c "import secrets; print(secrets.token_urlsafe(48))"
   ```
   انسخ الناتج.

2. في Coolify، افتح تطبيقك (web:main) → تبويب
   **Environment Variables** → أضف متغيّرين:

   | الاسم | القيمة |
   |-------|--------|
   | `JWT_SECRET` | السرّ الذي ولّدته |
   | `DATABASE_URL` | رابط PostgreSQL من الخطوة 2 |

3. احفظ.

────────────────────────────────────────────────────────
## الخطوة 4: ضبط إعدادات البناء والمنفذ
────────────────────────────────────────────────────────
1. في إعدادات التطبيق (Configuration):
   - **Build Pack**: Dockerfile (Coolify سيكتشف Dockerfile تلقائياً).
   - **Port**: 8000 (المنفذ الذي يستمع عليه التطبيق).
2. تأكد أن الدومين furanfakhar.com موجّه لهذا التطبيق (كان يعمل سابقاً).

────────────────────────────────────────────────────────
## الخطوة 5: إعادة النشر
────────────────────────────────────────────────────────
1. اضغط زر **Redeploy** (الأحمر فوق يمين، شفناه في صورتك).
2. راقب **Deployment Log** — يجب أن ينتهي بـ "Deployment is Finished".
3. افتح furanfakhar.com — يجب أن تظهر صفحة تسجيل الدخول الجديدة.

────────────────────────────────────────────────────────
## بعد النشر: التحقق
────────────────────────────────────────────────────────
- سجّل حساباً جديداً (القاعدة جديدة، فالحسابات القديمة غير موجودة).
- أضف فرناً، انسخ مفتاح الجهاز.
- عدّل الأردوينو: غيّر SERVER_HOST إلى furanfakhar.com والمنفذ 443
  (HTTPS) بدل localhost. (قد يحتاج تعديل بسيط للـ HTTPS — أخبرني).

────────────────────────────────────────────────────────
## تحديثات مستقبلية (سهلة جداً)
────────────────────────────────────────────────────────
أي تعديل لاحق:
```bash
git add . && git commit -m "وصف التعديل" && git push
```
Coolify ينشره تلقائياً (أو اضغط Redeploy).

────────────────────────────────────────────────────────
## ملاحظات أمنية مهمة
────────────────────────────────────────────────────────
- مفاتيح Pushover/Telegram القديمة (في server.py القديم) مكشوفة —
  غيّرها من حساباتك.
- ملف .env لا يُرفع على GitHub (محمي بـ .gitignore). المتغيّرات
  الحقيقية تُوضع في Coolify فقط.
- JWT_SECRET في الإنتاج يجب أن يكون مختلفاً وقوياً.
