# القطعة 2: الواجهة (بتصميم موقعك الأصلي)

> أُعيد بناء اللوحة بنفس تصميم موقعك الأصلي الجميل (ألوان متوهجة،
> رسم بياني، سجل، مراحل) لكن مربوطة بنظام الحسابات وتعدد المستخدمين.

## أهم تغيير: الحقول الحقيقية

الأردوينو يرسل حقولاً بأسماء مختصرة، وقد طابقناها تماماً:

| الحقل | المعنى |
|-------|--------|
| c1 | الحرارة الحقيقية |
| i1 | الحرارة الافتراضية |
| x  | الدرجة النهائية |
| H  | حالة البرنامج (0-5) |
| h  | الساعات الجارية |
| t  | وقت المرحلة |
| D / mD | مدة التثبيت / الجارية |
| ht / mt | ساعات/دقائق متبقية |
| x1..x3 / t1..t3 | حرارة ووقت المراحل |
| MARAHEL / DOWN | المراحل / النزول التدريجي |
| ElectricOff | خلل الحساس |
| wiresActive | حالة الأسلاك |

## الصفحات
- `/`    → دخول/تسجيل
- `/app` → قائمة أفراني، وعند اختيار فرن: لوحة التحكم الكاملة بتصميمك
- `/docs`→ توثيق الـ API

## ما الجديد في اللوحة
- نفس الشكل الأصلي: بطاقات الحرارة المتوهجة، الرسم البياني الحي،
  حالة البرنامج بالألوان، السجل، تفاصيل المراحل.
- لكل فرن مفتاح جهاز (زر "مفتاح الجهاز").
- التحديث كل 3 ثوانٍ.

## فرق مهم عن السيرفر القديم
السيرفر القديم (server.py) كان لفرن واحد، يخزّن في ملف log.json،
ويحتوي مفاتيح Pushover/Telegram مكشوفة في الكود. النظام الجديد:
- متعدد المستخدمين، كل فرن معزول.
- يخزّن في قاعدة بيانات.
- الإشعارات (Pushover/Telegram) سننقلها لاحقاً كميزة لكل مستخدم
  مع إخفاء المفاتيح في متغيّرات البيئة (مهم: مفاتيحك القديمة مكشوفة،
  يُنصح بتغييرها لأنها ظهرت في ملفات).

## كيف تجرّبها
```bash
uvicorn app.main:app --reload --port 8000
```
افتح `http://localhost:8000/` ← سجّل ← أضف فرناً ← انسخ المفتاح ←
أرسل قراءة كاملة:
```bash
curl -X POST http://localhost:8000/device/readings \
  -H "X-Device-Key: dvk_المفتاح" -H "Content-Type: application/json" \
  -d '{"c1":845.5,"i1":850,"x":1000,"H":2,"h":3.5,"MARAHEL":1,"DOWN":0,"ElectricOff":0,"wiresActive":"تضخ"}'
```
شاهد اللوحة تتحدّث بالرسم البياني والقيم.
