# القطعة 1-ج: الأفران والقراءات

> تُبنى فوق القطعتين 1-أ و1-ب. هذه القطعة تحقّق هدفك الأساسي:
> **كل مستخدم يرى أفرانه فقط**، والأردوينو يرسل قراءاته بمفتاح خاص.

## الملفات الجديدة

- `app/models/kiln.py` — جدولا `kilns` و `readings`.
- `app/models/kiln_schemas.py` — مخططات الأفران والقراءات.
- `app/core/device_auth.py` — التحقق من مفتاح الأردوينو (X-Device-Key).
- `app/routes/kilns.py` — كل مسارات الأفران والقراءات.
- `tests/test_kilns.py` — 12 اختباراً (أهمها اختبار العزل).
- `arduino_example.ino` — مثال إرسال من أردوينو R4.

## الفكرة الأمنية الأساسية

هناك نوعان من المصادقة منفصلان تماماً:

| من | كيف يصادق | ماذا يفعل |
|----|-----------|-----------|
| المستخدم | توكن JWT (تسجيل دخول) | يقرأ/يدير أفرانه |
| الأردوينو | مفتاح X-Device-Key | يرسل القراءات فقط |

**القاعدة الذهبية:** كل استعلام أفران يُفلتر بـ `owner_id == current_user.id`.
لذلك لا يمكن لمستخدم رؤية أو تعديل فرن غيره. لو حاول الوصول لفرن
ليس له، يحصل على 404 (لا نكشف حتى وجوده).

## المسارات

**للمستخدم (تحتاج توكن):**
- `POST /kilns` — إنشاء فرن. **يرجّع device_key مرة واحدة فقط** — احفظه.
- `GET /kilns` — قائمة أفراني.
- `GET /kilns/{id}` — تفاصيل فرن.
- `PATCH /kilns/{id}` — تعديل.
- `DELETE /kilns/{id}` — حذف.
- `GET /kilns/{id}/readings?limit=100` — القراءات (الأحدث أولاً).
- `GET /kilns/{id}/latest` — آخر قراءة (للوحة التحكم).
- `POST /kilns/{id}/rotate-key` — مفتاح جديد لو تسرّب القديم.

**للأردوينو (يحتاج X-Device-Key):**
- `POST /device/readings` — إرسال قراءة.

## كيف تجرّبها

```bash
# شغّل الاختبارات (يجب أن تنجح كلها)
pytest tests/ -v

# شغّل الخادم
uvicorn app.main:app --reload --port 8000
# افتح http://localhost:8000/docs
```

### تجربة يدوية من /docs
1. سجّل دخول، انسخ access_token، اضغط زر **Authorize** فوق والصقه.
2. `POST /kilns` باسم فرن — انسخ `device_key` من الرد.
3. `POST /device/readings`: اضغط Authorize ليس مطلوباً هنا؛ بدلاً منه
   استخدم curl أو أداة ترسل ترويسة X-Device-Key بالمفتاح.
4. `GET /kilns/{id}/latest` — سترى القراءة التي أرسلتها.

### مثال إرسال قراءة بـ curl
```bash
curl -X POST http://localhost:8000/device/readings \
  -H "X-Device-Key: dvk_المفتاح" \
  -H "Content-Type: application/json" \
  -d '{"temp_real": 845.5, "stage": 2, "wires_ok": true}'
```

## ربط الأردوينو الحقيقي
انظر `arduino_example.ino`. نقطة مهمة أثناء التجربة المحلية:
الأردوينو لا يفهم `localhost` (يعني نفسه)، فاستخدم **IP جهاز الماك**
في الشبكة (مثل 192.168.x.x). لمعرفته: إعدادات الشبكة في الماك.

## الخطوة التالية
- **القطعة 2:** الواجهة (صفحة دخول → قائمة أفراني → لوحة تحكم لكل فرن)
  تُركّب فوق هذه المسارات مباشرة.
- **القطعة 3:** الانتقال لـ PostgreSQL (تغيير DATABASE_URL فقط).
